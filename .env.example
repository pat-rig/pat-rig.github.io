# This is the public `site` repo. It is fully static and makes no API calls,
# so there are currently no environment variables required to build or run it.
#
# Do not add a Google Maps/Routes API key here. That key lives only in the
# private commute-logger repo's GitHub Actions secrets and must never appear
# in this repo, in client code, or in a committed .env file.
